[package]
name = "shapes"
version = "0.4.0"
loft = ">=2026.7.2"

api_compatible_with  = "0.3.0"   # oldest own release this is a drop-in for
data_compatible_with = "0.3.0"   # oldest own release whose stored data this reads
[library]
entry = "src/shapes.loft"
